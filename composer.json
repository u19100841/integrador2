{
    "name": "dev/tickets",
    "require-dev": {
        "phpunit/phpunit": "9"
    },
    "scripts":{
        "instalar":"composer install --ignore-platform-reqs",
        "test": "./vendor/bin/phpunit --testdox"
    }
}
